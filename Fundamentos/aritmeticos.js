const [a, b, c, d] = [3, 5, 1, 15] //quatro variaveis criadas

const soma = a + b + c + d
const subtraca = d -b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtraca, multiplicacao, divisao, modulo)
